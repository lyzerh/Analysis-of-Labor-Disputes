# Archived maintenance scripts

Historical one-off maintenance scripts retained for project provenance. They are not part of the current runtime, package scripts, CI workflow, or evaluation pipeline.

The current public workflow uses the scripts referenced by `package.json`, CI, and the documentation. Archived files should not be treated as supported commands without an explicit review.
